#!/usr/bin/env ruby

require 'implicitcad_watcher'

trap('SIGINT') { handle_sigint }

def handle_sigint
  # throw :ctrl_c
  puts 'Ctrl-C receied. Exiting.'
  exit 130
end

puts ImplicitCadWatcher.main(ARGV[0])
